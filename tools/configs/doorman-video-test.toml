# Test configuration for video file input
[daemon]
user_mode = true
debug_mode = true
preview_mode = true

[camera]
# Use video file instead of camera device
video_file = "/path/to/test_video.webm"
# Output resolution
width = 1024
height = 720
fps = 30

[ml]
backend = "tract"
device = "cpu"
